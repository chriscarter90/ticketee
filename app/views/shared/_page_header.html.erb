<div class="row page-titles">
  <div class='span12'>
    <% flash.each do |key, value| %>
      <div class = 'flash alert alert-warning' id='<% key %>'>
        <%= value %>
      </div>
    <% end %>
  </div>

  <div class='span3'>
    <%= content_tag :h1, yield(:title) %>
  </div>

  <div class='span9'>
    <% if yield(:functionality).present? %>
      <div id="dashboard-top-nav">
        <%= yield :functionality %>
      </div>
    <% end %>
  </div>
</div>

<hr>
